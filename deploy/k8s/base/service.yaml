apiVersion: v1
kind: Service
metadata:
  name: public-web
  namespace: di
  labels:
    app: public-web
spec:
  type: ClusterIP
  selector:
    app: public-web
  ports:
  - name: "8080"
    protocol: TCP
    port: 8080
    targetPort: 8080
